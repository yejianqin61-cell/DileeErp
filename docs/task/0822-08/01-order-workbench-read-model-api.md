# Task 01：G1 订单工作台读模型 API

新增只读订单工作台模块、控制器和统一权限保护，提供订单列表、订单详情和订单审计时间线接口。以 `SalesOrder.orderNo` 为根，支持分页、订单号、客户、状态和日期筛选；所有 Decimal 返回字符串，接口返回 `{ data, meta }`。

不得在该任务中修改任何业务事实或引入余额缓存。完成标准：API 类型检查和构建通过，未登录返回 401，无模块权限返回 403，未知订单返回 404。
