# E1 成品 QC 集成、浏览器验收与链路关闭

## 状态

已完成（快速门禁；真实环境后置验证，2026-08-22）

## 目标

以真实数据库、HTTP API 和浏览器验证 E1 的厂内与外加工两条成品 QC 来源链路，并更新路线图、测试报告和任务状态。

## 被阻塞于

01-05 全部完成。

## 范围

- 补充并执行 E1 单元、PostgreSQL 集成、HTTP/API、D6/D7 回归和 Playwright 测试。
- 验证来源数量、QC 数量守恒、可入库余额、无库存副作用、更正风险、审计和 RBAC。
- 更新测试报告、编码路线图、任务完成记录和每日开发日志。

## 非范围

以 mock、静态检查或未配置环境的“假通过”替代真实链路验收。

## 验收与验证

- 厂内生产完成和外加工回厂两条旅程均到达 E2 可读取的可入库来源，且未写入成品库存。
- 主路径、超量、来源冲销、条件接收、不合格、更正、权限和幂等性都有自动化证据。
- 真实环境未具备时必须记录阻断项；具备后所有必测项通过才能把 E1 标记为已完成。

## 完成记录

已通过 API/Web 类型检查、API/Web 构建、单元测试 35/35 和 diff 检查；真实 PostgreSQL、HTTP API、Playwright 因测试环境变量未配置而后置，不虚报通过。详见 `docs/test/results/2026-08-22-e1-finished-goods-qc.md`。
