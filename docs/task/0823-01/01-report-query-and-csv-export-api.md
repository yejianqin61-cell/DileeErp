# Task 01：G2 报表查询与 CSV 导出 API

新增统一报表模块，提供订单、采购/应付、库存、生产/QC、薪资报表查询和分页。复用 G1 与现有服务的事实来源，不创建报表余额缓存。新增 Excel 兼容 CSV 导出，带 UTF-8 BOM、RFC 4180 转义、固定列顺序、筛选条件和最大行数保护。

完成标准：统一响应/权限、Decimal 字符串、日期筛选、导出 content type 和超限错误均有测试，API 类型检查和构建通过。
