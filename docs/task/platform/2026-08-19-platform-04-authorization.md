# 平台任务 04：角色与模块权限

## 状态
已完成（数据库授权路径待 PostgreSQL）

## 依赖
平台任务 03、10。

## 目标

实现管理员与按模块授权的操作员权限模型。

## 范围

- 管理员角色。
- 六个模块权限键：`production`、`procurement`、`finance`、`warehouse`、`hr`、`customers`。
- 用户/角色/模块授权管理接口。
- NestJS 守卫或等效统一权限检查。
- 无权限统一返回 `403`。

## 非范围

不实现逐字段权限、复杂审批权限、数据行级权限或临时授权。

## 验收

- 管理员可授予和撤销模块权限。
- 操作员只能访问获授权模块。
- 撤销权限后新请求立即受限。
- 权限检查不依赖前端隐藏按钮，API 层必须强制执行。

## 完成记录

负责人：Codex
完成日期：2026-08-19
已完成：认证守卫、模块权限装饰器/守卫、六个模块权限键和管理员角色绕过规则。验证：API typecheck/build 通过。真实授予/撤销后的数据库访问控制验证待 PostgreSQL 可用后执行。
