# Task 03：E2 入库、不良品 API 与影响预览

## 目标

暴露仓库操作员可用的 REST JSON 接口。

## 实施要求

- 新增成品入库和不良品的 list/create/post/reverse 接口。
- 接入认证与 `warehouse` 模块权限，使用 DTO 校验 UUID、日期、数量和原因。
- 扩展 E1 QC 影响预览，返回已入库数量、已登记不良品数量和剩余数量。
- 遵循统一 `{ data, meta }` 响应格式。

## 完成标准

未登录返回 401、无仓库权限返回 403；合法请求可完成分批草稿和过账。
