# Task 02：E2 入库与不良品领域服务

## 目标

实现来源校验、分批余量、过账、幂等、冲销和审计事务。

## 实施要求

- 只接受 E1 active QC 记录；分别计算可入库量和可登记不良品量。
- 创建草稿、过账、冲销均使用 Decimal 和事务。
- 过账分别写入 `finished_goods`、`defective_goods` InventoryFact。
- 冲销写入负向事实并阻止负库存、重复冲销和重复过账。
- 提供来源影响预览所需的汇总方法。

## 完成标准

服务层可被控制器调用，所有失败分支返回项目统一异常结构，并有审计事件。
