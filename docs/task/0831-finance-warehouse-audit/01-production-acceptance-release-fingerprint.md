# 01 生产验收与发布指纹

## 状态
已完成

## 认领

负责人：Codex
开始日期：2026-08-31

## 目标
让生产部署能证明运行的是本次提交，且归档、依赖、迁移、构建和 PM2 启动失败时不切换旧版本。

## 范围
- 在发布流程增加提交/版本指纹和 API 健康检查字段。
- 校验归档根目录、环境变量端口、构建产物和 PM2 启动脚本。
- 增加真实 PostgreSQL、HTTP、权限和浏览器链路的验收命令与记录模板。

## 验收
- 服务器 API 返回的版本指纹等于归档 HEAD。
- 失败发布保留旧版本运行并留下失败目录。
- 两条最低业务链路有真实环境测试记录。

## 完成记录

- 2026-08-31：健康接口返回构建指纹；`git archive` 通过 `.gitattributes` 替换提交占位符；部署标准增加非 development 指纹检查。
- 验证：API 构建、单元测试通过；真实服务器验收待部署时执行。
