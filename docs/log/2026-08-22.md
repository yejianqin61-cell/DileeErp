# 2026-08-22 开发日志

## D5 生产日报、员工日报与告警链路

### task 01：领域模型与数据库 schema

- 完成四个 D5 领域模型：工序日报、员工日报、生产日报告警、生产薪资来源；
- 所有新表统一使用 UUID、审计字段和逻辑删除字段，并冗余保存 `order_no` 及快照字段；
- 新增迁移 `20260822120000_production_daily_reports_alerts_payroll_sources`，添加日报日期、业务查询索引、告警持续业务键和外键约束；
- 质量验证：Prisma format/validate/generate 与 API build 通过；
- PostgreSQL 部署验证并入 task 07 的统一回归，未在本 task 中伪造通过结论。

### task 02：工序日报与工序进度 API

- 新增工序日报 REST API：列表、详情、新增、修改、逻辑删除、影响预览；
- 新增生产单工序累计进度查询，服务端计算目标量、累计量、差额、超单量和状态；
- 写入边界覆盖厂内生产、有效工序、生产单状态、正数量和非未来日期；
- 日报写入/修改/删除在事务内触发超单重算，并保留统一审计事件；
- 验证：API build 通过，PostgreSQL 接口回归列入 task 07。
