# 2026-08-30

## Completed

- 形成《采购收货、来料质检与可逆入库改进设计》规格文档，覆盖工序池和加工地点二级页面、日报编号冲突、采购分批到货/应付、来料质检和可逆入库六项改进。
- 统一采用领域术语“收货批次、来料质检批次、原料入库批次、应付来源”，并明确使用可重复记录而不是动态数据库列。

## Decisions

- 采购单详情作为收货流程主入口；到货确认幂等生成来料质检任务。
- 质检和入库均按采购批次追溯，库存过账后只能通过冲销回退，不能静默覆盖历史事实。
- 工序池、加工地点、收货批次、质检批次和入库批次继续使用现有组件库。

## Verification

- 已核对领域术语词典、生产/采购既有设计文档和部署/工程规范。
- GitHub 远程仓库为 `yejianqin61-cell/DileeErp`；当前 `gh` 凭据无效且联网发布被环境安全审查拦截，issue 尚未创建。

## Next

- GitHub 登录恢复后，使用规格文档创建 issue 并添加 `ready-for-agent` 标签；用户确认设计后再拆分实施任务，暂不编码。
