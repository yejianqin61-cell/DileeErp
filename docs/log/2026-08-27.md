# 2026-08-27

## 完成

- 规范化了远程 Ubuntu 服务器的增量部署流程，新增 `.agent` 内的部署标准文档。
- 记录了这次部署中必须先判断数据库拓扑、再恢复原数据卷、再执行迁移和 PM2 重启的顺序。

## 结论

- 部署现场不能默认只有一套 PostgreSQL。
- 生产数据卷与应用 `.env` 必须同步核对，不能用猜测的连接串替代。

## 下步

- 后续每次部署先按标准文档核对数据库拓扑，再执行源码替换和服务重启。
