# 2026-09-15

## Completed

### 第五轮（银行账户池补界面入口）

用户问「银行池在哪？我咋没看见」——排查结论：**后端全套都在（banks 表 + `GET/POST/PATCH/PATCH toggle/
DELETE /finance/banks`），前端一个页面都没有**，只把它当「支付银行」下拉的数据源；财务一级页也没有入口。
于是账户只能靠接口写、下拉永远是空的。设计见 `docs/design/bank-account-pool-page-2026-09-15.md`。

- 财务一级页新增第 7 个板块「银行账户」→ `/finance/banks`（`page-finance-banks`）；
- 新增 `components/finance/bank-workspace.tsx`：满页表格（编码/名称/账户名/账号/币种/SWIFT/状态/备注）、
  本地搜索、启用计数、新建与编辑（币种取币种字典）、行内停用/启用、删除二次确认、空态指路；
- 停用是主操作（移出下拉、保留档案），删除是软删除（已引用的付款/对账仍显示账户名）；
- 页面上写明它与「结算账户字典」的区别（前者是主数据 `bank_id`，后者是老表结算方式的字典项
  `settlement_account_id`，无外键关系）；
- 应付页两个「支付银行」字段标签改成「（可选；账户在『财务 → 银行账户』里维护）」，从使用处反向指路；
- 顺带发现并记录：`components/ui/alert-dialog.tsx` 在当前依赖版本下**一渲染就抛 Radix
  `Primitive.div failed to slot onto its children`**（隔离实验证明该包装本身有问题；它是全站唯一
  没有任何调用方的 UI 组件，所以一直没暴露）。本次不依赖它，二次确认改用全站已验证的 `Dialog` 原语。

### 第四轮（生产进度表：工序列顺序可拖拽）

用户要求：「导出生产进度表时允许拖拽调整各工序排序，导出的生产进度表的工序 column 按这个顺序排」。
设计见 `docs/design/production-progress-column-order-2026-09-15.md`。

- 列顺序定位为**导出显示偏好**，不去改 `production_order_operations.sequence_no`（那是车间实际生产顺序，
  两者是不同事实）；顺序随导出请求的 `operation_order`（逗号分隔工序 id）传，后端只按它排表头；
- 后端新增 `production-progress-columns.domain.ts`（`parseOperationOrder` / `orderProgressColumns`，
  纯函数）：用户列出的按给定次序在前，**没提到的按原相对顺序接在后面**（存过顺序也不会让新增工序丢列）；
  导出元信息在用到自定义顺序时记录 `operation_order`；
- 生产进度表用独立的 `ProgressExportDto`：原料对应表/订单号盘点表收到 `operation_order` 会 400，
  而不是静默忽略（沉默会让人以为列序生效了）；
- 前端新增 `lib/progress-column-order.ts`（纯逻辑）与 `components/production/progress-column-order-editor.tsx`
  （原生 HTML5 拖拽 + 上移/下移按钮 + 恢复默认顺序 + 拖拽中高亮），内嵌进导出面板的生产进度表弹窗；
  顺序按订单号记在 localStorage（与面板折叠状态同一套做法），**只有顺序真的变了**才往 URL 加参数；
- 数量口径完全不变：只重排 columns 数组，目标数量/加工地点/每日数量/表尾合计都跟着同一批列走。

### 第三轮（应付流转 + 应付对账列 + 收支流水覆盖）

用户三条反馈：①「点接受应付没有反应，没有流转到应付对账」「对账创建完也没有流转到确认付款 ——
这个流转的步骤最重要」；② 应付对账要加「该批原料对应订单号」与「采购的物料名称」列；
③ 排查收支流水收纳了什么（「没有办法把全部的收支纳入进去」）。
设计见 `docs/design/payable-flow-and-cash-flow-coverage-2026-09-15.md`。

- **应付流转**（三处断点都修了）：
  1. 对账的系统余额原先只算**已确认**应付，而它要确认的正是草稿 → 改成含草稿
     （`ENTRY_STATUSES` 单一来源，与 get/confirmPayables/前端分组一致）：接收完应付去做对账
     才能真的对平；
  2. 「到确认应付确认 N 条」的提示原先读**详情**接口的 details（列表不返回）→ 改成列表返回
     `flow` 摘要（覆盖条数/待确认条数/订单号/采购单号/物料），提示与按钮都靠它；
  3. 「批量确认该对账范围的应付」接口原先**没有任何界面入口** → 对账行上新增
     「确认 N 条应付」按钮（对平/差异已处理且有草稿时出现），成功提示回报确认条数与金额；
  4. 另加顶部**四步流转看板**（待接收来源 → 待确认草稿 → 已对平待确认 → 已确认未付），
     每步给数量与入口链接、卡住的那步高亮；接收应付的成功提示也点名下一步。
- **应付对账新列**：待创建对账分组加 订单号 / 采购物料；已创建对账单加 订单号 / 采购单号 /
  采购物料 / 待确认应付（条数 + 金额）；对账详情的纳入明细加 采购物料 / 采购单号。
- **收支流水**（用户要的排查结论 + 修复）：
  - 排查结论：自动写入只有三处（客户收款、供应商付款、工资付款），而**供应商付款写死的项目 key
    「外加工费」在字典里不存在**（内置 37 项里是「外加工费 晋江大田工资」），当时缺项直接 `return null`
    → **每一笔供应商付款都被静默丢掉**，流水里只剩客户货款 + 工资付款 + 手工录入；
  - 修复一：项目解析改为**候选链**（`PAYMENT_ITEM_KEYS` / `paymentItemKeys()`），按来源选
    原料成本 / 成品外加工费 / 管理费用；候选全缺时**显式 422**，不再静默丢弃；
  - 修复二：结算方式按老表格式带上银行（`转账--农业银行5706`），并把银行名 + 账号作为
    `settlementAccountHint` 交给收支流水；`matchSettlementAccount()` 按「账号数字一致 + 银行名互相包含」
    匹配到**结算账户字典**（老表 `农业银行5706` / `中国银行（美元）7624`），匹配不上留空
    （银行来自 `banks` 表、结算账户来自字典，没有外键关系，不造假关联）；对方名称不再退化成 UUID；
  - 修复三：新增 `autoReverseFromPayment`，收付款冲销时把对应流水置为 `reversed`（原先只写过账不处理冲销，
    收支汇总会比银行账多一笔）；
  - 仍未自动纳管的部分（房租水电差旅备用金、退款实际出账、退税利息等）如实写进设计文档，
    需要新模块（费用/退款单）才能自动进流水。

### 第二轮（用户追加要求：入口页 + 行内付款）

用户看完第一轮后追加两条：① 工资台账和工资付款做成全屏表格 UI，**工资管理页只提供这两个功能入口**，
其余全部收到对应二级页；② 工资付款应当**直接把当月工资台账搬过去**，但 column 只保留总工资，
付款操作都在表格中完成。

- 页面结构：`/finance/salary` 变成纯入口页（两个板块卡片，本身**不发任何请求**），
  新增 `/finance/salary/ledger`（工资台账）与 `/finance/salary/payments`（工资付款）两个二级页；
  用真实路由而不是查询参数切 tab —— 两个表格都要占满整屏，地址也能直接收藏。
- 工资付款表 = 当月工资台账的付款视图：同一套筛选与按月导入，但**类目列全部收掉**，
  只留 总工资/已付/未付/状态/币种，外加付款日期与付款方式（整页统一设置）。
- 行内付款：`POST /hr/payroll-ledgers/:id/pay` 一次完成「生成工资应付 → 确认应付 → 建付款草稿 →
  核销过账」，操作员只需在行上填金额点一次；金额先按台账实发与已付余额校验（超付 422 且不产生
  任何单据），后续任何一步失败都会把本次草稿**软删除并审计**，不留孤儿单据。
- 行内冲销：`POST /hr/payroll-ledgers/:id/unpay` 把该台账下所有已过账付款逐张走既有 `reverse()`
  （每张各自刷新台账/应付状态、回冲现金流），原因必填。
- 不可付款的行仍然显示（当月台账全都在），只是把输入框换成原因：待确认 / 已过期 / 已付清。
- 工资付款页不再有「新建付款草稿」入口：草稿无法自行过账，只会堆积；接口保持可用。

### 第一轮（工资台账可编辑 + 生产工资自动汇总）

- 完成用户第 3、4 条需求「工资管理」：工资台账从只读表改成**可编辑满页表格**，工资付款拆成独立
  子栏目，两个表格共用「月份 / 部门 / 岗位 / 员工姓名或工号」筛选。设计见
  `docs/design/payroll-ledger-monthly-sheet-2026-09-15.md`，任务见
  `docs/task/0914-finance-module-refactor/09-salary-ledger-sheet.md`。
  1. **每月自动导入全部员工**：新增 `POST /hr/payroll-ledgers/import-month`（幂等，只补建缺失的草稿台账），
     进页面与切月份自动调一次；导入响应回传候选人数、新建/已有/不在职计数与逐人生产工资覆盖度
     （日报条数 / 天数 / 单数 / 工序数）。
  2. **车间生产工资按日报自动汇总**：新增 `production-payroll.domain.ts`，按「日期 × 生产单 × 工序 ×
     计薪方式」汇总员工日报，取数**不做订单/工序预筛**，保证「不能漏掉任何一单任何一个工序任何一天」；
     金额落在「基本工资」格且对车间工人只读。非车间员工金额全为 0。
  3. **类目改造**：新增 房补 / 迟到扣款 / 旷工扣款 / 早退扣款 四列（迁移
     `20260915140000_payroll_ledger_salary_categories`），绩效复用原列；老类目（加班/考勤扣款/补贴/
     社保/个税/其他调整）收进只读的「其他增减」格，避免「应发 ≠ 表上可见列之和」的黑洞。
  4. **逐格编辑**：新增 `components/finance/payroll-sheet.tsx`（单击编辑、Enter 保存、Esc 取消、Tab 右移、
     方向键换格、清空=0、值没变不打接口、失败标红并显示原因）。
  5. **工资付款满页表格**：`GET /hr/salary-payments` 新增 `month/department_id/position_id`，
     按**核销到的员工**命中部门/岗位。
- 顺带修掉 4 个真问题：
  1. **应发公式在 5 处各写一遍**（列表 balances、详情 summary、付款后 refreshStatus、工资应付 netAmount、
     工资付款过账内联算式）：加类目只改一处就会让同一张台账在不同页面金额不一致，现已收敛到
     `payrollBaseAmount()` 一处；
  2. **`summary` 少算了「工资付款已过账」**：同一张台账在列表与详情给出不同已付/未付；
  3. **生产工资取派生表会漏单**：`production_payroll_sources` 在员工类型车间→非车间时被软删、改回车间不补建；
  4. **软删台账重导入会 500**：唯一索引 `(employee_id, period_start, period_end)` 把软删行也算在内，
     导入前必须连软删一起读（既有 `generate` 在软删后重生成仍有这个坑）。
- 前端另一个坑顺手修掉：`ActionDialog` 的 onSubmit 必须把错误抛回去，否则失败时弹窗静默关闭、
  用户看不到原因（工资页的六个弹窗全部改成 `submitDialog`）。

## Decisions

- 老列**不改语义、只加新列**：`allowance_amount`（补贴）不复用成「房补」，`attendance_deduction`（考勤扣款）
  不拆成三种扣款再写回；新增四列的唯一代价是一次 `ALTER TABLE`，换来历史金额不被重新解释。
- 6 个可编辑类目之外的老类目必须**显示**（只读「其他增减」）：藏起来就会出现应发与可见列之和对不上。
- 车间工人的 `base_salary` 手工改成非零 → 422（生产工资就是基本工资，两个来源必然漂移）；
  但允许清零，给历史数据留一条清理路。
- 表格内只有 `draft` / `expired` 可改；已确认台账格子上写明「先回到草稿」，整条编辑仍在弹窗里填原因。
- 「每个月自动先导入全部员工」落地为**幂等导入 + 进页面自动触发**：浏览即写库是用户明确要的行为，
  重复访问同一月份零写入。该月不在职（任职区间与该月无交集）的员工不导入，但会计数回传。
- 薪资构成类目的后台增删改（2026-08-20 备忘第 7 条「灵活为上」）确认是**后期**能力，本次不做：
  固定 6 个类目 + 只读兜底格已能覆盖当前诉求。
- （第二轮）工资管理页**只做入口**、功能进二级页，且用真实路由而不是查询参数：两个表格都要占满整屏、
  地址可收藏，也就不需要「切 tab 保留筛选」这类补丁；入口页不发任何请求，进错页不会触发按月导入写库。
- （第二轮）工资付款**只保留总工资**：付款只关心「这个月该发多少、已经发了多少」，类目明细留在台账页，
  两页同行数同行来源（同一套筛选与导入），不会出现「付款页看到的人与台账页不一样」。
- （第二轮）付款的四步链路收成**一次行内调用**，但每一步仍复用既有服务、没有另写金额或状态判断；
  失败时软删除本次草稿 —— 付款单没有删除接口、只有过账后能冲销，孤儿草稿会一直挂在列表里。
- （第二轮）冲销按**台账**聚合（该台账下所有已过账付款逐张走既有 reverse），而不是让操作员去付款单列表里找，
  避免出现「台账已冲销一半」的状态。
- （第三轮）**应付对账必须含待确认草稿**：这是「先对账、再确认应付」的前提；应收侧本来就含草稿
  （`status: { not: "cancelled" }`），应付侧只算已确认属实现偏差，按应收口径对齐。
- （第三轮）**流转必须在列表行上可见可点**：摘要走列表接口而不是详情接口 —— 用户不点开详情就该知道
  自己卡在哪一步、下一步点哪里。
- （第三轮）**资金动账缺字典项时，报错优于静默丢弃**：规则是「每一笔过账的收付款都要在流水里有落点」，
  做不到就得让人看到（422 + 需要哪些项目 + 去哪补），而不是悄悄把账做丢。
- （第三轮）**不自动创建收支项目**：项目是管理员可维护的业务类目，程序自动往字典里塞会让字典失控；
  缺项时给可执行的报错。
- （第四轮）生产进度表的**列顺序是导出显示偏好，不是生产顺序**：偏好只走导出参数 + 本机记忆
  （localStorage，按订单号），不写回 `sequence_no`、不新增表；没提到的工序按原相对顺序排在后面，
  存过顺序不会让新增工序丢列；只有生产进度表认这个参数，其它导出收到就 400 而不是静默忽略。

## Verification

- `npm run typecheck`（api + web）→ 通过。
- `npm run test:unit:api` → **1113 / 1113 通过**（第一轮新增生产工资聚合 8 条 + 迁移守卫 5 条，
  第二轮新增行内付款/冲销编排 8 条，第三轮新增 对账口径与 flow 摘要 3 条、收付款自动写流水与回冲 5 条、
  项目映射 3 条、结算账户匹配 4 条，第四轮新增 工序列顺序 domain 5 条 + 进度表按序排列 3 条）。
- 前端组件测试 → **34 个文件 / 611 个用例全绿**（第三轮新增 `test/payable-page.test.tsx` 10 条；
  第四轮在 `payroll-export-panel.test.tsx` 新增 5 条：默认顺序不带参数、下移后 `operation_order` 按新顺序、
  原生拖拽路径（jsdom 需显式传 `dataTransfer` 才会触发 React 的拖拽事件）、恢复默认后参数消失且
  按订单号记忆、无工序时的提示；并给该文件的 afterEach 补上 `localStorage.clear()`
  —— 不清理会让前一个用例存的顺序变成后一个用例的初始顺序，本次实测踩到；
  第五轮新增 `test/bank-page.test.tsx` 10 条覆盖银行账户池）；lib **134 条**全绿。
- 环境备注（非代码问题）：本机今天多次出现 V8 内存抖动 —— `next build` 一次 `Committing semi space
  failed`、组件测试在默认 4 worker 下多次 `Zone Allocation failed`/`process out of memory`。
  同一套用例用 `npx vitest run --maxWorkers=2` **一次跑完、全绿**（74.9s），未改动任何测试配置。
- **未执行**：`npm run db:migrate:deploy`（本机无可用 PostgreSQL）、`test:api:raw`（HTTP 合约层）、
  `test:integration`（真实库层）、Playwright。应付流转与收支流水的真实 SQL、并发与鉴权矩阵尚未端到端验证。

## Next

- 在可用 PostgreSQL 上验一遍完整应付流转：原料入库过账 → 接收应付 → 应付对账（系统余额应等于
  草稿合计）→ 行内「确认 N 条应付」→ 登记付款 → 过账 → 收支流水出现该笔支出 → 冲销付款 →
  流水同步变 `reversed`。
- 历史数据修补：升级前被静默丢掉的供应商付款在收支流水里是**缺的**，需要按收付款单据回补
  （建议一次性脚本：遍历 `supplier_payments` 中 `status=posted` 且无对应流水的单据，
  用本次的候选链补写，并标注「历史回补」）。
- 若财务希望房租/水电/差旅这类经常性支出也自动进流水，需要新的费用单模型（本次未做）。
